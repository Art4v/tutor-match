// Metadata-only server layout so the client reset-password page gets a tab title.
export const metadata = { title: "Reset password" };

export default function ResetPasswordLayout({ children }) {
  return children;
}
