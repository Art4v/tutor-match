// Metadata-only server layout so the client forgot-password page gets a tab title.
export const metadata = { title: "Reset password" };

export default function ForgotPasswordLayout({ children }) {
  return children;
}
